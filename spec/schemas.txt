# Component schemas the SDK provides a typed model for, checked against the
# vendored spec (spec/openapi.json, pinned by .api-version) by
# scripts/check-spec-drift.mjs. The check validates this list BOTH ways:
#
#   * every name here must exist in the spec's components.schemas (no
#     removed/renamed/typo'd schema), AND must be exported as a matching model
#     in src/models.ts (the list can't claim coverage the code doesn't have);
#   * every schema in the spec must be listed here, or the check fails with a
#     coverage gap — so a NEW upstream schema can't land unnoticed and unmodeled.
#
# So: when the pinned spec gains or renames a schema, update both this file and
# src/models.ts, or the drift check fails. One schema name per line.
LoginRequest
LoginResponse
Market
MarketSummary
MarketStatus
MarketRiskParams
AdlClosureRecord
AdlEventRecord
Ticker
OrderBook
Trade
FundingSample
OrderRequest
AmendOrderRequest
OrderResponse
OrderResult
OrderResultOk
OrderResultErr
Order
OrderHistoryEntry
PreviewResponse
AccountSummary
AccountPortfolioSummary
AccountFunding
EquityPoint
CreditRequest
CreditResponse
FaucetResponse
RateLimitStatus
CancelOnDisconnectStatus
SetCancelOnDisconnectRequest
Position
ClosedPosition
AgentRegistrationRequest
AgentInfo
Fill
Withdrawal
FundsEntry
DepositRequest
DepositResponse
BridgeError
BridgeAsset
BridgeChainAssets
BridgeAssetsResponse
CreateBridgeDepositAddressRequest
BridgeDepositAddress
BridgeDeposit
StatsSnapshot
ThroughputSample
ServiceHealth
Decimal
TimestampMs
