# Component schemas the SDK provides a typed model for, checked against the
# vendored spec (spec/openapi.json, pinned by .api-version) by
# scripts/check-spec-drift.mjs. The check validates this list BOTH ways:
#
#   * every name here must exist in the spec's components.schemas (no
#     removed/renamed/typo'd schema), AND must be exported as a matching model
#     in src/models.ts (the list can't claim coverage the code doesn't have);
#   * every schema in the spec must be listed here, or the check fails with a
#     coverage gap — so a NEW upstream schema can't land unnoticed and unmodeled.
#
# So: when the pinned spec gains or renames a schema, update both this file and
# src/models.ts, or the drift check fails. One schema name per line.
LoginRequest
LoginResponse
Market
MarketSummary
MarketStatus
MarketRiskParams
AdlClosureRecord
AdlEventRecord
Ticker
OrderBook
Trade
FundingSample
FundingPremiumSample
OrderRequest
AmendOrderRequest
OrderResponse
OrderResult
OrderResultOk
OrderResultErr
Order
OrderHistoryEntry
PreviewResponse
AccountSummary
AccountPortfolioSummary
AccountFunding
EquityPoint
AccountState
AccountFees
FeeDiscount
PortfolioWindow
PortfolioPoint
PortfolioHistory
CreditRequest
CreditResponse
FaucetResponse
RateLimitStatus
CancelOnDisconnectStatus
SetCancelOnDisconnectRequest
Position
ClosedPosition
AgentRegistrationRequest
AgentInfo
Fill
Withdrawal
FundsEntry
DepositRequest
DepositResponse
BridgeError
BridgeAsset
BridgeChainAssets
BridgeAssetsResponse
CreateBridgeDepositAddressRequest
BridgeDepositAddress
BridgeDeposit

# Withdrawal wallets (v0.7.3). Two-step ownership proof; no client method yet.
CreateBridgeWalletChallengeRequest
BridgeWalletChallenge
RegisterBridgeWalletRequest
BridgeWallet
BridgeWalletsResponse

StatsSnapshot
ThroughputSample
ServiceHealth

# Liquidations (v0.7.3) — payloads of the per-account `liquidations` WS channel.
LiquidationEvent
LiquidationAlert
PortfolioLiquidation
PortfolioLiquidationClosure

# Network discovery (v0.7.3) — the edge's `/metadata` payload. Deliberately not an
# operation in the contract, so these are models only.
Metadata
NetworkTarget
SigningDomain

# Jurisdiction controls (v0.7.3) — the 403 body on state-changing operations.
JurisdictionError

Decimal
TimestampMs
