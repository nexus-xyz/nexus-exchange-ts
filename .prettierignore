dist/
node_modules/
package-lock.json
pnpm-lock.yaml
# Vendored upstream spec — preserve it verbatim, don't reformat.
spec/openapi.json
# Generated by release-please, which emits "*" bullets and double blank lines
# that prettier rewrites. The release PR is the one PR with no CI (it is opened
# with GITHUB_TOKEN, so its workflows never trigger), so a reformat-only diff
# there is invisible until the release lands and turns main red. Fixed by hand
# once already, in #34's "style: prettier-format the release-please CHANGELOG".
CHANGELOG.md
